"""Desktop-facing resource orchestration over finesub_bootstrap.

The download/extract/install primitives themselves live in
``src/finesub_bootstrap``; this package keeps the desktop-only layers:
the bridge-facing service facade and the pausable install manager with
its UI snapshots.
"""
